databaseChangeLog:
  - changeSet:
      id: create-tables
      author: f11h
      changes:
        - createTable:
            tableName: quicktest
            columns:
              - column:
                  name: id
                  type: varchar(32)
                  constraints:
                    unique: true
                    nullable: false
                    primaryKey: true